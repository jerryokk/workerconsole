/**
 * WorkerConsole - 本地 Cloudflare Workers 管理系统
 * 
 * 这是应用程序的入口点，负责启动服务器
 */

// 导入服务器
const server = require('./server');

// 服务器已在 server.js 中启动，这里不需要额外操作
console.log('WorkerConsole 服务已启动'); 